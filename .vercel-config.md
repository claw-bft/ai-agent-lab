# Vercel 部署配置

## 状态
- [ ] Vercel Token 已配置
- [ ] 可执行非交互式部署

## 配置步骤
1. 访问 https://vercel.com/account/tokens 创建 Token
2. 设置环境变量: `export VERCEL_TOKEN=your_token`
3. 运行: `vercel --token=$VERCEL_TOKEN --yes --prod`

## 当前限制
- 当前环境未配置 VERCEL_TOKEN
- 需要交互式登录（浏览器）

## 替代方案（当前使用）
- 使用 localtunnel 临时部署: https://dry-tires-poke.loca.lt
- 或使用 GitHub Pages

## 已记住的规则 ✅
- 所有部署任务**默认使用 Vercel**
- 部署前检查 VERCEL_TOKEN 是否配置
- 如未配置，提示用户或记录到 TODO
